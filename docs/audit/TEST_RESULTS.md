# AgentSentinel — Test Results

**Audit Date:** 2026-05-05  
**Methodology:** Code review (static analysis), Python test suite execution, and manual scenario walkthroughs. Automated E2E (Playwright) is not yet in place — see P2-2 in [ROADMAP.md](ROADMAP.md).

---

## Phase 7.1 — Unit Test Coverage

| Test Area | File / Command | Status | Evidence |
|---|---|---|---|
| License key HMAC signing (Python) | `python/tests/test_licensing_parity.py` | ✅ Pass | 336 tests pass: `cd python && python -m pytest tests/ -v` |
| HMAC cross-language parity (TS ↔ Python) | `python/tests/test_licensing_parity.py` + `python/tests/fixtures/license-vectors.json` | ✅ Pass | Test vectors generated by TypeScript edge function and verified by Python |
| License key format validation | `python/tests/test_licensing_parity.py` | ✅ Pass | `asv1_*`, `as_<tier>_*` accepted; malformed keys rejected |
| Tier limits (`TIER_LIMITS`) | `supabase/functions/_shared/tiers.ts` | ✅ Pass | 6 tiers: `free`, `starter`, `pro`, `pro_team`, `team`, `enterprise` — canonical definitions |
| Rate limiter sliding window | `supabase/functions/_shared/rate-limit.ts` | ✅ Pass (by inspection) | Sliding window implementation prunes expired timestamps correctly |
| Config check (Python) | `python/agentsentinel/config_check.py` | ✅ Pass | Required env vars validated; masked output for secrets |
| Config check (Deno) | `supabase/functions/_shared/config-check.ts` | ✅ Pass | Mirror of Python check for Edge Function env |

**Overall unit test result:** ✅ **336/336 pass**

To reproduce:
```bash
cd python && python -m pytest tests/ -v
```

---

## Phase 7.2 — Integration Tests Checklist

| Integration | Test Method | Status | Notes |
|---|---|---|---|
| Stripe webhook → `webhook_events` insert | Code review: `stripe-webhook/index.ts` + migration `012` | ✅ Pass | `INSERT … ON CONFLICT(stripe_event_id) DO NOTHING` |
| Webhook replay (idempotency) | Code review: `stripe-webhook/index.ts` | ✅ Pass | Duplicate event returns 200 without processing |
| `validate-license` → DB lookup + audit log | Code review: `validate-license/index.ts` | ✅ Pass | `license_validations` row inserted with SHA-256 hash |
| `validate-promo` → DB lookup + validation chain | Code review: `validate-promo/index.ts` | ✅ Pass | Checks `active`, `expires_at`, `max_uses`, `tier` |
| `admin-generate-promo` → `promo_codes` insert | Code review: `admin-generate-promo/index.ts` | ✅ Pass | Bearer auth required; 6-tier `VALID_TIERS` set used |
| OTP send → `portal_otps` upsert | Code review: `send-portal-otp/index.ts` | ✅ Pass | Rate-limited; enumeration-resistant |
| OTP verify → license fetch | Code review: `customer-portal/index.ts` | ✅ Pass | Constant-time OTP comparison; expires_at checked |
| Python SDK online validation | `python/agentsentinel/licensing.py` | ✅ Pass | Calls `validate-license` EF; parses tier + limits |
| Python SDK offline HMAC validation | `python/agentsentinel/licensing.py` + `utils/keygen.py` | ✅ Pass | Falls back to cached signed key when offline |
| DB schema migrations (in order) | `supabase/migrations/` — 001 through 012 | ✅ Pass | No conflicts; `IF NOT EXISTS` guards on indexes |

---

## Phase 7.3 — E2E Manual Test Scenarios

The following scenarios were traced through code. Automated Playwright coverage does not exist yet.

| # | Scenario | Expected Result | Status |
|---|---|---|---|
| E1 | Visitor visits `index.html` → clicks CTA → arrives at `pricing-team.html` | Page loads, plan cards visible | ✅ Traced |
| E2 | Customer enters valid promo code on pricing page | `validate-promo` returns `{valid:true, type, value}` | ✅ Traced |
| E3 | Customer enters invalid/expired promo code | `{valid:false, reason:'expired'}` or `'not_found'` | ✅ Traced |
| E4 | Customer selects plan → redirected to Stripe Checkout | `checkout-team` creates session; redirect URL returned | ✅ Traced |
| E5 | Payment completes → Stripe sends `checkout.session.completed` | Webhook processes, license row created, email sent | ✅ Traced |
| E6 | Webhook event replayed by Stripe | Duplicate insert blocked by `ON CONFLICT`; 200 returned | ✅ Traced |
| E7 | Customer visits `success.html` → clicks "Go to Portal" | OTP email sent via Resend | ✅ Traced |
| E8 | Customer enters valid OTP in portal | License key displayed in-memory (not localStorage) | ✅ Traced |
| E9 | Customer's OTP exceeds 5 attempts | `429 Too Many Requests` with `Retry-After` header | ✅ Traced |
| E10 | Developer sets `AGENTSENTINEL_LICENSE_KEY` and calls `guard()` | Online validation returns tier + limits | ✅ Traced |
| E11 | SDK used offline with signed key | HMAC verified locally; `valid=True` returned | ✅ Traced |
| E12 | SDK sends invalid license key | `validate-license` returns `{valid:false, error:"Invalid license key"}` | ✅ Traced |
| E13 | `validate-license` called 21 times in 60 s from same IP | 21st request returns `429` with `Retry-After: 60` | ✅ Traced |
| E14 | Admin logs in to admin dashboard | `sessionStorage` populated; service-role key never hits DOM | ✅ Traced |
| E15 | Admin creates promo code | `admin-generate-promo` called with Bearer token; row inserted | ✅ Traced |
| E16 | Admin revokes a license | License status set to `revoked`; audit log entry created | ✅ Traced |
| E17 | Admin views webhook events | Badge shows `pending`/`processed`/`failed` correctly | ✅ Traced (M-1 fix) |
| E18 | Admin views overview tier breakdown | All 6 tiers show correct counts | ✅ Traced (M-3 fix) |

---

## Phase 7.4 — Performance Targets

| Target | SLA | Verified | Method | Notes |
|---|---|---|---|---|
| Admin dashboard load | < 2 s | ⚠️ Not measured | No test harness | SPA loads ~15 JS files; no bundler — P2-3 |
| `validate-promo` response | < 200 ms | ⚠️ Not measured | No load test | Code path is a single DB lookup; expected < 100 ms in practice |
| `validate-license` response | < 150 ms | ⚠️ Not measured | No load test | Single DB lookup + audit insert; expected < 120 ms |
| Webhook processing | < 5 s | ⚠️ Not measured | No load test | Involves DB writes + Resend API call |

**Verdict:** Performance targets are not formally verified. Add k6/locust test harness (P2-2 in roadmap) before making public SLA commitments.

---

## Phase 7.5 — Security Tests

| Check | Status | Method | Evidence / Notes |
|---|---|---|---|
| `ADMIN_API_SECRET` required for `admin-generate-promo` | ✅ Pass | Code review | `admin-generate-promo/index.ts` returns 401 if `Authorization` header missing or wrong |
| Supabase service-role key not in any HTML file | ✅ Pass | `grep -r SUPABASE_SERVICE_ROLE_KEY *.html` | Not found in HTML |
| License keys not in `localStorage` | ✅ Pass | Code review `portal.html` | In-memory state only |
| OTP brute-force protection | ✅ Pass | Code review `send-portal-otp/index.ts` | 3 sends + 5 verifies per email/15 min; 429 + `Retry-After` |
| Email enumeration resistance | ✅ Pass | Code review | Same HTTP response for existing and non-existing email addresses |
| Stripe webhook signature verification | ✅ Pass | Code review `stripe-webhook/index.ts` | `stripe.webhooks.constructEvent()` used; invalid signatures → 400 |
| SQL injection protection | ✅ Pass | Code review | Supabase JS client uses parameterized queries throughout |
| CSRF protection (admin) | ✅ Pass | Code review | Admin API requires `Authorization: Bearer` header (SameSite cookies not used) |
| Sensitive fields masked in `admin_logs` | ✅ Pass | Code review `api.js` `_maskSensitive()` | Keys/tokens hashed to SHA-256 prefix before logging |
| `validate-license` key format validation | ✅ Pass | Code review | Rejects keys not starting with `asv1_` or `as_<valid_tier>_` |
| Tier enum guard (stale/corrupt DB data) | ✅ Pass | Code review `validate-license/index.ts:164` | Returns 500 for unrecognized tier values |
| CORS on `validate-license` | ✅ Pass | Code review | Restricted to `https://agentsentinel.net` |
| No secrets in source code | ✅ Pass | `grep -r "sk_live\|sk_test\|service_role" .` | No hardcoded secrets found |
| `.env.example` vs actual secrets | ✅ Pass | Code review | `.env.example` uses placeholder values only |
